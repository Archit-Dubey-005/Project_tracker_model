services:
  - type: web
    name: ipt-matcher-api
    runtime: docker
    plan: free
    region: oregon
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8000
      - key: API_SECRET_KEY
        generateValue: false
